
@{
    ViewData["Title"] = "Menu";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div id="PartialView">
</div>
<script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
<script src="https://cdn.datatables.net/1.11.5/js/jquery.dataTables.js"></script>
    <script>
        function Onload() {
            $.post("/Menu/AllItemList").done(function (res) {
                $("#PartialView").html(res);
            }).fail(function () {
                alert("errror");
            })
        }
        Onload();
        function EditMenu() {
            $.post("/Menu/EditMenu").done(function (res) {
                $("#PartialView").html(res);
            }).fail(function () {
                alert("errror");
            })
        }

        function EditOrAdd(Id) {
            $.post('Menu/foodkart', { EmpId: Id }).done(function (res) {
                $("#PartialView").html(res);
            }).fail(function () {
                alert("Error");
            })
        }
        function ViewDetails(Id) {
            $.post('Home/Edit', { EmpId: Id }).done(function (res) {
                $("#PartialView").html(res);
            }).fail(function () {
                alert("Error");
            })
        }
        function TeaKart() {
            $.post("/TeaKart/AllTeaKartList").done(function (res) {
                $("#PartialView").html(res);
            }).fail(function () {
                alert("errror");
            })
        }
    </script>


